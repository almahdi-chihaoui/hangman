import template from './headerTemplate.html'
import headerStyle from './headerStyle.sass'

const bindings = {
  
}

export const headerComponent = {
  controller: ['$scope', 'gameEngine', function($scope, gameEngine){
  
  }],
  template,
  bindings
}